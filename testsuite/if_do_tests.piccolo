program if_do_tests "PIC18F448" :

routine main noreturn {
  if (INCF FSR0L NZ)
    CLRF WREG
  end
  if (DECF FSR0L, W Z)
    mnop 1024
  end
  if (STATUS == W)
    CLRF STATUS
  end
  if (STATUS != W)
    CLRF STATUS
  end
  if (STATUS >= W)
    CLRF STATUS
  end
  if (STATUS < W)
    CLRF STATUS
  end
  if (STATUS <= W)
    CLRF STATUS
  end
  if (STATUS > W)
    CLRF STATUS
    mnop 1023
  end

  if (STATUS Z)
    mnop 2
  end
  if (STATUS NZ)
    mnop 2
  end
  if (STATUS Z)
    CLRF STATUS
    if (STATUS NZ)
      mnop 1024
    end
    CLRF STATUS
  end
  forever
  end
}

end
